import { DelSpaceDirective } from './del-space.directive';

describe('DelSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new DelSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
